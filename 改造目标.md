# Thunder 框架：StepState 回调模式改为 C++20 协程

## 目标(最关键部分)

Thunder（在wsl 的 目录 /home/administrator/thunder） 是一个基于 libev 的全异步 C++ 网络框架，当前使用 **StepState 状态机模式** 处理异步操作：每个状态函数执行一个异步操作，返回 `STATUS_CMD_RUNNING`，等待框架回调后进入下一个状态。这种模式导致：
- 业务逻辑分散在多个 State 函数中（State0/State1/State2...）
- 状态跳转靠 `SetNextState()`/`JumpNextState()`，流程难以一目了然
- 需要手动注册状态函数数组

目标：用 C++20 协程（`co_await`/`co_return`）替换 StepState 模式，使异步代码写起来像同步代码。

## 改造效果对比(最关键部分)

```cpp
// ===== 改造前（StepState，5个状态函数）=====
class StepHttpRequestState : public StepState {
    StepHttpRequestState(...) {
        AddStateFunc((StepStateFunc)&StepHttpRequestState::State0);
        AddStateFunc((StepStateFunc)&StepHttpRequestState::State1);
        // ...
    }
    E_CMD_STATUS State0() { HttpGet("http://baidu.com"); return RUNNING; }
    E_CMD_STATUS State1() { HttpGet("http://sogou.com"); SetNextState(4); return RUNNING; }
    E_CMD_STATUS State4() { Response(0); return COMPLETED; }
};

// ===== 改造后（C++20 协程，单个线性函数）=====
class HttpRequestCo : public CoroutineState {
    CoTask Run() override {
        auto resp1 = co_await HttpGetAsync("http://baidu.com");
        auto resp2 = co_await HttpGetAsync("http://sogou.com");
        Response(0);
        co_return;
    }
};
```

其他所有step改成类似HttpRequestCo的方式
可以参考 drogon的协程处理方式（在wsl 的 目录 /home/administrator/drogon）

todo list：
使用cmake构建
支持使用docker运行
只保留需要的第三方库
优化简化代码，使用c++17、20新特性
