# Thunder 框架：StepState 回调模式改为 C++20 协程

## 目标(最关键部分)

Thunder（在wsl 的 目录 /home/administrator/thunder） 是一个基于 libev 的全异步 C++ 网络框架，当前使用 **StepState 状态机模式** 处理异步操作：每个状态函数执行一个异步操作，返回 `STATUS_CMD_RUNNING`，等待框架回调后进入下一个状态。这种模式导致：
- 业务逻辑分散在多个 State 函数中（State0/State1/State2...）
- 状态跳转靠 `SetNextState()`/`JumpNextState()`，流程难以一目了然
- 需要手动注册状态函数数组

目标：用 C++20 协程（`co_await`/`co_return`）替换 StepState 模式，使异步代码写起来像同步代码。

## 改造效果对比(最关键部分)

```cpp
// ===== 改造前（StepState，5个状态函数）=====
class StepHttpRequestState : public StepState {
    StepHttpRequestState(...) {
        AddStateFunc((StepStateFunc)&StepHttpRequestState::State0);
        AddStateFunc((StepStateFunc)&StepHttpRequestState::State1);
        // ...
    }
    E_CMD_STATUS State0() { HttpGet("http://baidu.com"); return RUNNING; }
    E_CMD_STATUS State1() { HttpGet("http://sogou.com"); SetNextState(4); return RUNNING; }
    E_CMD_STATUS State4() { Response(0); return COMPLETED; }
};

// ===== 改造后（C++20 协程，单个线性函数）=====
class HttpRequestCo : public CoroutineState {
    CoTask Run() override {
        auto resp1 = co_await HttpGetAsync("http://baidu.com");
        auto resp2 = co_await HttpGetAsync("http://sogou.com");
        Response(0);
        co_return;
    }
};
```

其他所有step改成类似HttpRequestCo的方式
可以参考 drogon的协程处理方式（在wsl 的 目录 /home/administrator/drogon）

todo list：
使用cmake构建
支持使用docker运行
只保留需要的第三方库
优化简化代码，使用c++17、20新特性
Step 改造为 CoroutineState （保持上下文），后续去掉step的概念，使用CoroutineState+ 非阻塞  socket + libev /或者 CoroutineState + 线程池 + 被调用api ，同步的写法实现来异步请求  

# 详细改造计划

## 🎯 核心理念
**同步写法 + 异步执行**：使用 C++20 协程让异步代码看起来像同步代码

## 📍 架构演进路线

### 阶段 1：过渡期（保持 Step 概念兼容）
**目标**：平滑迁移，不破坏现有业务

```cpp
// 当前：StepState（回调地狱）
class StepHttpRequest : public StepState {
    E_CMD_STATUS State0() { /* 异步操作 */ return RUNNING; }
    E_CMD_STATUS State1() { /* 处理结果 */ return RUNNING; }
};

// 过渡：CoroutineState（继承 Step，保持上下文）
class HttpRequestCo : public CoroutineState {  // 仍然是 Step
    CoTask Run() override {
        auto resp = co_await HttpGetAsync("url");  // 同步写法
        co_return resp;
    }
};
```

**保持的内容**：
- Labor 调度机制
- Step 生命周期管理
- 现有的错误处理
- 超时机制

### 阶段 2：目标架构（去掉 Step 概念）
**目标**：纯协程驱动，简化框架

```cpp
// 最终：纯 CoroutineState（不再继承 Step）
class HttpRequestCo : public CoroutineState {
    CoTask Run() override {
        auto resp = co_await HttpGetAsync("url");
        co_return resp;
    }
};

// 直接调用，不需要 Step 注册
auto result = co_await HttpRequestCo().Run();
```

## 🔧 两种异步实现方式

### 方式 1：CoroutineState + 非阻塞 socket + libev（推荐）
**适用场景**：网络 I/O（HTTP、Redis、MySQL）

**架构设计**：
```cpp
class CoroutineState {
protected:
    Labor* m_labor;  // 保持 libev 事件循环上下文
    
    // 非阻塞 socket 包装器
    template<typename T>
    Awaitable<T> AsyncIO(int fd, IOType type) {
        return Awaitable<T>([this, fd, type](auto handle) {
            // 1. 注册 libev 事件
            ev_io* watcher = new ev_io;
            ev_io_init(watcher, callback, fd, type);
            ev_io_start(m_labor->GetLoop(), watcher);
            
            // 2. 保存协程句柄
            watcher->data = handle.address();
            
            // 3. 挂起协程，返回事件循环
            return std::suspend_always{};
        });
    }
};

// 使用示例
CoTask HttpGetAsync(const std::string& url) {
    int sockfd = CreateNonBlockingSocket(url);
    
    // 同步写法，异步执行
    co_await AsyncIO<void>(sockfd, EV_WRITE);  // 等待可写
    Send(sockfd, request);
    
    co_await AsyncIO<void>(sockfd, EV_READ);   // 等待可读
    auto response = Recv(sockfd);
    
    co_return response;
}
```

**优势**：
- 零拷贝，高性能
- 充分利用 libev 的多路复用能力
- 单线程异步，无锁

### 方式 2：CoroutineState + 线程池 + 被调用 API（备选）
**适用场景**：阻塞 API（文件 I/O、计算密集型任务）

**架构设计**：
```cpp
class ThreadPoolExecutor {
public:
    template<typename Func>
    Awaitable<std::invoke_result_t<Func>> Execute(Func&& func) {
        return Awaitable([this, func = std::forward<Func>(func)](auto handle) {
            // 1. 提交任务到线程池
            m_pool.Submit([func, handle]() {
                auto result = func();  // 在工作线程中执行阻塞操作
                
                // 2. 结果准备好后，通知主线程恢复协程
                PostToMainThread([handle, result]() {
                    handle.resume();
                });
            });
            
            return std::suspend_always{};
        });
    }
};

// 使用示例
CoTask FileReadAsync(const std::string& path) {
    ThreadPoolExecutor executor;
    
    // 同步写法，后台线程执行
    auto content = co_await executor.Execute([path]() {
        return ReadFileBlocking(path);  // 阻塞 API
    });
    
    co_return content;
}
```

**优势**：
- 兼容所有阻塞 API
- 不需要改造第三方库
- CPU 密集型任务友好

## 📋 实施计划时间线

### 第一阶段：过渡期实现（4-6 周）

#### 1.1 CoroutineState 基础设施
- [ ] 创建 `CoroutineState` 基类（继承 `Step`，保持上下文）
- [ ] 实现 `CoTask` 协程返回类型
- [ ] 实现 `Awaitable<T>` 模板（可等待对象）
- [ ] 集成 libev 事件循环

#### 1.2 非阻塞 I/O 协程化
- [ ] `AsyncSocket` - 封装非阻塞 socket + libev
- [ ] `HttpGetAsync()` / `HttpPostAsync()`
- [ ] `RedisGetAsync()` / `RedisSetAsync()`
- [ ] `MysqlQueryAsync()`

#### 1.3 线程池协程化（可选）
- [ ] `ThreadPoolExecutor` - 线程池管理
- [ ] `AsyncFileIO` - 文件操作
- [ ] `AsyncCompute` - CPU 密集型任务

#### 1.4 Step 改造示例
- [ ] 改造 3 个典型 Step（HTTP、Redis、Mysql）
- [ ] 提供改造前后对比
- [ ] 编写改造指南

### 第二阶段：去 Step 化（6-8 周）

#### 2.1 重构核心框架
- [ ] CoroutineState 不再继承 Step
- [ ] 实现纯协程调度器（替代 Labor 的部分功能）
- [ ] 简化生命周期管理

#### 2.2 API 简化
- [ ] 移除 `AddStateFunc()`、`SetNextState()` 等遗留接口
- [ ] 统一错误处理（使用异常或 `std::expected`）
- [ ] 简化超时机制

#### 2.3 批量迁移
- [ ] 迁移所有业务 Step
- [ ] 移除旧的 StepState 代码
- [ ] 更新文档

### 第三阶段：构建与部署现代化（2-3 周）

- [ ] CMake 构建系统
- [ ] Docker 支持
- [ ] 精简第三方库
- [ ] 应用 C++17/20 新特性
- [ ] 测试与文档

## 🔑 关键技术点

### 1. 协程挂起与恢复
```cpp
struct Awaitable {
    bool await_ready() { 
        return false;  // 总是挂起
    }
    
    void await_suspend(std::coroutine_handle<> handle) {
        // 注册到 libev，事件触发时调用 handle.resume()
        RegisterToLibev(handle);
    }
    
    T await_resume() { 
        return m_result;  // 返回异步操作结果
    }
};
```

### 2. 保持 Labor 上下文
```cpp
class CoroutineState {
protected:
    Labor* GetLabor() { return m_labor; }  // 访问事件循环
    Codec* GetCodec() { return m_codec; }  // 访问编解码器
    // ... 其他上下文
};
```

### 3. 错误处理
```cpp
CoTask HttpGetAsync(const std::string& url) {
    try {
        co_await AsyncConnect(url);
        co_await AsyncSend(request);
        auto resp = co_await AsyncRecv();
        co_return resp;
    } catch (const TimeoutException& e) {
        // 处理超时
    } catch (const NetworkException& e) {
        // 处理网络错误
    }
}
```

## ✅ 验收标准

1. **代码简洁度**：5 个 State 函数 → 1 个 Run() 协程
2. **性能**：与原 StepState 相当或更优
3. **兼容性**：过渡期保持现有业务稳定运行
4. **可维护性**：新人能快速理解协程代码

## 📊 当前进度跟踪

### 已完成
- [x] 需求分析与现有代码分析
- [x] WSL 环境搭建与测试
- [x] 简单协程功能验证
- [x] 构建问题修复
- [x] **集成测试基础设施**（第一阶段完成）
  - [x] 容器化测试环境配置（Dockerfile.test, docker-compose.test.yml）
  - [x] Google Test 集成（CMakeLists.txt 更新）
  - [x] 基础测试框架类（IntegrationTestBase）
  - [x] 测试数据清理机制
  - [x] 测试运行脚本和报告生成工具
  - [x] 外部服务模拟支持（WireMock, MockServer）
  - [x] HTML 测试报告生成
  - [x] 资源限制配置

### 进行中
- [x] CoroutineState 基础设施（`CoTask`/`HttpGetAsync`/`WaitForAsync`，继承 `HttpStep`）
- [x] `net::Launch(Step*)` 支持 `CoroutineState`（与 `StepState` 分流 Init / SetTimeoutParams）
- [x] Hello 示例：`TestHttpRequestState` 默认使用 `HttpRequestCo`；`TestHttpRequestStateMachine` / `TestHttpRequestStateFuncLegacy` 保留旧 StepState 对照
- [x] `TestHttpRequestStateFunc` 改为 `HttpStateFuncCo`（协程线性逻辑）
- [ ] 构建系统现代化（根 CMake 仍为精简版；完整工程以既有构建为准）

### 待开始
- [ ] 批量 Step 改造
- [ ] 去 Step 化重构
- [ ] 性能测试与优化
