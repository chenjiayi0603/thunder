# System-wide core dump placement
kernel.core_pattern=/home/chen/thunder/core/core-%e-%p-%t
# Append PID to core name
kernel.core_uses_pid=1
# Allow dumps for suid binaries if needed (0/1)
fs.suid_dumpable=1
