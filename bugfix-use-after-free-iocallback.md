# Bug 修复案例：IoCallback 中的 heap-use-after-free

**项目**：Thunder 框架 Net 库  
**涉及组件**：Interface / Logic / Manager / Worker  
**Bug 类型**：heap-use-after-free → 堆腐败（heap corruption）→ SIGSEGV  
**修复文件**：`Manager.cpp`、`Worker.cpp`、`Labor.cpp`

---

## 一、背景与现象

### 1.1 问题描述

对 Interface 服务跑集成测试（`deploy/tests/test_interfaceserver.sh`）时，`Interface_robot_W0` Worker 进程在处理 HTTP 请求后随机崩溃。客户端表现：

```
curl: (52) Empty reply from server
```

崩溃后约 1 秒 Manager 重新 fork 出新 Worker，服务恢复，但每次 HTTP 请求都触发一次进程级别的崩溃重启。

---

## 二、第一步：查日志，确认崩溃位置

### 2.1 查看 Interface Worker 日志

```bash
tail -60 deploy/Interface/log/Interface_robot_W0.log
```

**关键输出**：

```
[INFO]  Interface_robot program begin, pid(75888)       ← 旧 Worker 启动
[INFO]  Init() pid(75888) listen on iPortForServer(16078)
[INFO]  Interface_robot program begin, pid(75889)       ← Worker 子进程
...
[TRACE] RecvDataAndDispose()
[TRACE] pBuff->ReadableBytes() = 359:POST /VerifyKey ...
[TRACE] Decode()
[TRACE] Decode()
                                                        ← 日志在此处突然中断，无报错
[INFO]  Interface_robot program begin, pid(75914)       ← 新 Worker 被重启，PID 变了
```

**判断**：日志无 ERROR/WARN 即中断，排除业务主动关闭连接，PID 变化说明是**进程崩溃后被 Manager 重新 fork**。

### 2.2 查看 Manager 日志确认

```bash
grep -E "FATAL|signal|duty.*exit" deploy/Interface/log/Interface_robot.log | tail -10
```

**输出**：

```
[FATAL] error 6: duty 75889 exit and sent signal 17 with code 6!
```

- `signal 17`：SIGCHLD（子进程终止信号，由内核通知父进程）
- `code 6`：杀死子进程的信号是 6（SIGABRT）或退出码 6

### 2.3 查看 dmesg

```bash
dmesg | tail -20
```

**输出**：

```
[timestamp] signal: 11
[timestamp] general protection fault in libc.so.6
[timestamp] Interface_robot[75889]: segfault at ... in libc.so.6
[timestamp] WSL CaptureCrash ...
```

- `signal: 11` = **SIGSEGV**（段错误，非法内存访问）
- `general protection fault in libc.so.6`：崩溃指令在 libc 内部，不是业务代码直接越界，而是**业务代码把损坏的指针传给了 libc**（如 `memcpy`/`strlen`），是堆腐败的典型特征

---

## 三、第二步：获取 core 文件

### 3.1 检查当前 core_pattern

```bash
cat /proc/sys/kernel/core_pattern
```

**输出**：

```
|/wsl-capture-crash %t %E %p %s
```

WSL 默认把 core 通过管道传给 `wsl-capture-crash`，**不落到磁盘**。需要修改。

### 3.2 临时切换为磁盘 core

```bash
sudo sysctl -w kernel.core_pattern=core.%p
```

**输出**：

```
kernel.core_pattern = core.%p
```

`%p` 表示 core 文件名里包含进程 PID，方便定位。

### 3.3 重新触发崩溃

```bash
cd deploy/tests && bash test_interfaceserver.sh
```

### 3.4 找到 core 文件

```bash
find deploy/ -name "core.*" 2>/dev/null
```

**输出**：

```
deploy/Interface/core.75889
```

> Worker 的工作目录是 `deploy/Interface/`，所以 core 落在这里。

### 3.5 验证 core 有效性

```bash
file deploy/Interface/core.75889
```

**输出**：

```
deploy/Interface/core.75889: ELF 64-bit LSB core file, x86-64, version 1 (SYSV),
SVR4-style, from 'Interface_robot_W0',
real uid: 1000, effective uid: 1000,
execfn: '/home/administrator/interview-quicker/thunder/deploy/Interface/bin/Interface',
platform: 'x86_64'
```

确认：core 来自 `Interface_robot_W0`，对应可执行文件路径已知，可以直接喂给 gdb。

---

## 四、第三步：gdb 分析 core

### 4.1 安装 gdb

```bash
which gdb
# 输出：Command 'gdb' not found
sudo apt install gdb
```

### 4.2 加载 core 打印完整回溯

```bash
gdb -q -batch \
  -ex 'set pagination off' \
  -ex 'thread apply all bt full' \
  deploy/Interface/bin/Interface \
  deploy/Interface/core.75889 2>&1
```

**参数说明**：
- `-q`：不打印 gdb 启动 banner
- `-batch`：非交互模式，执行完 `-ex` 命令后退出
- `-ex 'set pagination off'`：关闭分页，防止输出被截断
- `-ex 'thread apply all bt full'`：打印所有线程的完整回溯（含局部变量）

**关键输出**（精简）：

```
Core was generated by `Interface_robot_W0'.
Program terminated with signal SIGSEGV, Segmentation fault.
#0  __memcpy_avx_unaligned_erms ()
    at ../sysdeps/x86_64/multiarch/memmove-vec-unaligned-erms.S:344

Thread 1 (Thread 0x7de906... (LWP 75889)):
#0  __memcpy_avx_unaligned_erms ()
#1  0x...71e in std::char_traits<char>::copy (
        __s1=0x65746e492f205453 <error: Cannot access memory at address 0x65746e492f205453>,
        __s2=0x7de905e36820 "log//Interface_robot_W0.log",
        __n=<optimized out>)
#4  std::__cxx11::basic_string<char,...>::_M_replace (
        this=0x7de905e1e708,
        ...
        __p = 0x65746e492f205453 <error: Cannot access memory>
#8  log4cplus::spi::InternalLoggingEvent::setLoggingEvent (
        this=this@entry=0x7de905e1e6e0,
        logger="log//Interface_robot_W0.log",
        ...)
        ev = @0x7de905e1e6e0: {
            _vptr.InternalLoggingEvent = 0x292865646f63000a,  ← vtable 已损坏！
            message = <error: Cannot access memory>,
            loggerName = <error: Cannot access memory at 0x65746e492f205453>
        }
#11 net::HttpCodec::Decode (
        this=0x7de905e5eb00,
        pBuff=0x7de905eb7500, ...)
        at /home/.../code/Net/src/codec/HttpCodec.cpp:75
        ← LOG4_TRACE("%s()", __FUNCTION__)
#12 net::HttpCodec::Decode (this=0x..., pConn=0x...)
        at HttpCodec.cpp:58
#13 net::Worker::RecvDataAndDispose (this=0x..., pData=0x...)
        at Worker.cpp:473
#14 net::Worker::IoCallback (loop=..., watcher=0x..., revents=1)
        at Worker.cpp:81
#15 ev_invoke_pending (loop=...) at ev.c:3288
#16 ev_run (loop=..., flags=0) at ev.c:3688
#18 net::Manager::CreateWorker (this=...) at Manager.cpp:1204
#19 net::Manager::Manager (...) at Manager.cpp:165
#20 main (...) at basic_string.tcc:242
```

### 4.3 解读崩溃地址

崩溃目标指针：`0x65746e492f205453`

把这个 8 字节值**按小端序**展开为内存字节数组，再转 ASCII：

```
内存中的字节（低地址→高地址）：53  54  20  2F  49  6E  74  65
对应 ASCII 字符：               S   T  (空) /   I   n   t   e
```

即 `"ST /Inte"`，正是 HTTP 请求行 `POST /Interface...` 中间的一段原始字节。

**结论**：`log4cplus::InternalLoggingEvent` 堆对象的 `loggerName` 字符串内部指针字段，被 HTTP 报文的原始数据覆写了。崩溃在 `LOG4_TRACE` 只是"踩雷"，真正的"埋雷"（越界写）发生在更早的调用里。需要 **ASan** 定位第一次越界写的位置。

### 4.4 查看 HttpCodec.cpp 第 75 行上下文

```bash
sed -n '73,107p' code/Net/src/codec/HttpCodec.cpp
```

**输出**：

```cpp
E_CODEC_STATUS HttpCodec::Decode(util::CBuffer* pBuff, MsgHead& oMsgHead, MsgBody& oMsgBody)
{
    LOG4_TRACE("%s()", __FUNCTION__);   // ← 第 75 行，崩溃就在这里
    if (pBuff->ReadableBytes() == 0)
    {
        LOG4_TRACE("no data...");
        return(CODEC_STATUS_PAUSE);
    }
    HttpMsg oHttpMsg;
    E_CODEC_STATUS eCodecStatus = Decode(pBuff, oHttpMsg);
    ...
}
```

崩溃在函数**第一行**，说明在进入此函数之前，堆对象已经被破坏了。

---

## 五、第四步：ASan 构建，定位越界写

### 5.1 为什么需要 ASan

gdb 分析 core 只能看到"踩雷"时刻的状态，不知道"谁、什么时候、在哪里写坏了内存"。

AddressSanitizer 的工作原理：
- 在每次 `malloc` 前后插入不可访问的"红区"（redzone），并用毒值标记
- `free` 后把该内存块标记为"已释放"（poisoned，shadow byte = `fd`）
- 每次内存读写都插入检测代码，一旦访问 poisoned 区域立即 abort 并打印三条调用链：**分配位置**、**释放位置**、**非法访问位置**

### 5.2 新建 ASan 构建目录

不覆盖正常的 `build/` 目录，单独建 `build_asan/`：

```bash
cmake \
  -DCMAKE_BUILD_TYPE=Debug \
  -DCMAKE_CXX_FLAGS="-fsanitize=address -fno-omit-frame-pointer -g" \
  -DCMAKE_C_FLAGS="-fsanitize=address -fno-omit-frame-pointer -g" \
  -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=address" \
  -DCMAKE_SHARED_LINKER_FLAGS="-fsanitize=address" \
  -B build_asan
```

```bash
cmake --build build_asan --target Center --target Hello
```

- `--target Center`：编译注册中心服务
- `--target Hello`：cmake 别名，同时编译 Logic 和 Interface 两个服务

**编译输出（关键行）**：

```
Deploy: deploy/Center/bin/Center
Deploy: deploy/Logic/bin/Logic + deploy/Interface/bin/Interface
[100%] Built target Hello
```

### 5.3 遇到问题：ASan 运行库加载顺序错误

运行测试脚本后，Center 无法启动：

```bash
cat deploy/Center/log/test_interfaceserver.log
```

**输出**：

```
==84279==ASan runtime does not come first in initial library list;
you should either link runtime to your application or
manually preload it with LD_PRELOAD.
```

**排查原因**：

```bash
# 检查 deploy/lib/ 里的 libUtil.so 是否被 ASan 版本污染
ldd deploy/lib/libUtil.so | grep asan
```

**输出**：

```
libasan.so.8 => /lib/x86_64-linux-gnu/libasan.so.8
```

**原因**：cmake 构建 ASan 版本时，`libUtil.so`（带 ASan）通过 cmake 的 `install` 逻辑被复制到 `deploy/lib/`。测试脚本设置 `LD_LIBRARY_PATH` 指向 `deploy/lib/`，所以未用 ASan 编译的 Center 在加载时拿到了 ASan 版的 `libUtil.so`。

进一步验证 Interface 二进制不依赖 `deploy/lib/libUtil.so`：

```bash
ldd deploy/Interface/bin/Interface | grep -E "asan|Util"
```

**输出**：

```
libasan.so.8 => /lib/x86_64-linux-gnu/libasan.so.8
libUtil.so => /home/administrator/.../build_asan/lib/libUtil.so   ← 走 RPATH
```

Interface 通过编译期写入的 RPATH 直接找到 `build_asan/lib/libUtil.so`，不走 `deploy/lib/`，没有问题。

**解决**：把 `deploy/lib/` 里的共享库恢复为正常（非 ASan）构建版本：

```bash
cp build/lib/libUtil.so  deploy/lib/libUtil.so
cp build/lib/libNet.so   deploy/lib/libNet.so
cp build/lib/libProto.so deploy/lib/libProto.so

# 验证已不含 ASan
ldd deploy/lib/libUtil.so | grep asan || echo "clean - no asan"
```

**输出**：

```
clean - no asan
```

### 5.4 直接运行 Logic 触发 ASan 报告

用 Logic（与 Interface 共享同一份 Net 代码）单独测试，隔离干扰：

```bash
CODE_ROOT=/home/administrator/interview-quicker/thunder/code
DEPLOY_ROOT=/home/administrator/interview-quicker/thunder/deploy
export LD_LIBRARY_PATH="${DEPLOY_ROOT}/lib:${CODE_ROOT}/3party/lib:${CODE_ROOT}/3party/lib/mariadb:${CODE_ROOT}/3party/protobuf/build"

cd deploy/Logic
ASAN_OPTIONS=log_path=/tmp/asan_logic timeout 8 ./bin/Logic conf/Logic.json &
sleep 5
ls /tmp/asan_logic.*
```

**输出**：

```
/tmp/asan_logic.88995
```

> **`ASAN_OPTIONS=log_path`** 的作用：ASan 默认写 stderr，但服务进程 fork 后父子进程共享 stderr 文件描述符，输出会混乱或丢失。设置 `log_path=/tmp/asan_logic` 后，ASan 按 PID 写独立文件（`/tmp/asan_logic.88995`），确保每个进程的报告完整保存。

### 5.5 读取 ASan 报告

```bash
cat /tmp/asan_logic.88995
```

**完整输出**：

```
=================================================================
==88995==ERROR: AddressSanitizer: heap-use-after-free
         on address 0x50200000c070 at pc 0x56bfed37e050

READ of size 4 at 0x50200000c070 thread T0 (Logic_robot)
    #0 net::Manager::IoWrite(tagManagerIoWatcherData*, ev_io*)
       Manager.cpp:420                    ← 非法读：读 freed pData->iFd
    #1 net::Manager::IoCallback(ev_loop*, ev_io*, int)
       Manager.cpp:70
    #2 ev_invoke_pending  ev.c:3288
    #3 ev_run             ev.c:3688
    #5 net::Manager::Run() Manager.cpp:581
    #6 main               main.cpp:24

0x50200000c070 is located 0 bytes inside of 16-byte region
[0x50200000c070, 0x50200000c080)    ← 16 字节，就是 tagManagerIoWatcherData 的大小

freed by thread T0 (Logic_robot) here:
    #0 operator delete(void*, unsigned long)
    #1 void net::Labor::DelEvent<ev_io, tagManagerIoWatcherData>(ev_io*, tagManagerIoWatcherData*)
       Labor.hpp:682                      ← SAFE_DELETE(pData)
    #2 net::Manager::DestroyConnect(...)
       Manager.cpp:1612
    #3 net::Manager::RecvDataAndDispose(tagManagerIoWatcherData*, ev_io*)
       Manager.cpp:411                    ← ECONNREFUSED 触发
    #4 net::Manager::IoRead(...)
       Manager.cpp:225
    #5 net::Manager::IoCallback(ev_loop*, ev_io*, int)
       Manager.cpp:66                     ← 同一个 IoCallback！

previously allocated by thread T0 (Logic_robot) here:
    #0 operator new(unsigned long)
    #1 net::Manager::AddIoReadEvent(tagConnectionAttr*)
       Manager.cpp:1411                   ← new tagManagerIoWatcherData
    #2 net::Manager::AutoSend(...)        Manager.cpp:825
    #3 net::Manager::ReportToCenter(bool) Manager.cpp:1856
    #4 net::Manager::OnChildTerminated(ev_signal*)  Manager.cpp:208

SUMMARY: AddressSanitizer: heap-use-after-free
Manager.cpp:420 in net::Manager::IoWrite(...)
```

**ASan Shadow Map（关键行）**：

```
Shadow byte legend:
  Freed heap region: fd   ← 0xfd 表示已释放的堆内存

=>0x50200000c000: fa fa fd fa fa fa fd fa fa fa 00 00 fa fa[fd]fd
                                                            ↑
                                          0x50200000c070 处的 shadow byte = fd
                                          表示这块内存已被 free，但代码还在读它
```

**三条调用链的含义**：

| 链 | 关键帧 | 含义 |
|----|--------|------|
| 非法读（访问） | `IoWrite` → `Manager.cpp:420` | 读 `pData->iFd`，但 pData 已释放 |
| 释放 | `DestroyConnect` → `DelEvent` → `SAFE_DELETE(pData)` | 因 ECONNREFUSED 销毁连接时释放了 pData |
| 分配 | `AddIoReadEvent` → `new tagManagerIoWatcherData` | 为新建连接分配 watcher 数据 |

最关键的发现：**释放链和访问链都来自 `Manager.cpp` 的 `IoCallback`**，一个在第 66 行（IoRead 调用），一个在第 70 行（IoWrite 调用）——是**同一个函数调用里的先后两行代码**。

---

## 六、第五步：阅读相关代码，理解根因

### 6.1 查看 IoCallback 代码

```bash
sed -n '58,92p' code/Net/src/labor/Manager.cpp
```

**输出**：

```cpp
void Manager::IoCallback(struct ev_loop* loop, struct ev_io* watcher, int revents)
{
    if (watcher->data != nullptr)
    {
        tagManagerIoWatcherData* pData = (tagManagerIoWatcherData*)watcher->data;
        Manager* pManager = pData->pManager;
        if (revents & EV_READ)
        {
            pManager->IoRead(pData, watcher);   // line 66: 可能 free pData
        }
        if (revents & EV_WRITE)
        {
            pManager->IoWrite(pData, watcher);  // line 70: 使用已 free 的 pData
        }
        if (revents & EV_ERROR)
        {
            pManager->IoError(pData, watcher);
        }
    }
}
```

**问题一目了然**：当 `revents = EV_READ | EV_WRITE` 时，`IoRead` 内部调 `DestroyConnect` 释放了 `pData`，但代码继续执行到 `IoWrite(freed pData)`。

### 6.2 查看 AddIoReadEvent 和 AddIoWriteEvent，确认共用一个 pData

```bash
sed -n '1399,1465p' code/Net/src/labor/Manager.cpp
```

**输出（精简）**：

```cpp
bool Manager::AddIoReadEvent(tagConnectionAttr* pConn)
{
    if (nullptr == pConn->pIoWatcher)        // 第一次调用
    {
        ev_io* io_watcher = new ev_io();
        tagManagerIoWatcherData* pData = new tagManagerIoWatcherData();
        pData->iFd    = pConn->iFd;
        pData->ulSeq  = pConn->ulSeq;
        pConn->pIoWatcher = io_watcher;
        io_watcher->data  = pData;           // pData 挂在 watcher->data
        AddEvent(EV_READ, io_watcher, IoCallback, pData->iFd);
    }
    else                                     // watcher 已存在，仅追加 EV_READ
        RefreshEvent(pConn->pIoWatcher->events | EV_READ, pConn->pIoWatcher);
}

bool Manager::AddIoWriteEvent(tagConnectionAttr* pConn)
{
    if (nullptr == pConn->pIoWatcher)        // watcher 不存在才新建
    {
        // 同上，新建 watcher 和 pData
    }
    else                                     // watcher 已存在，仅追加 EV_WRITE
        RefreshEvent(pConn->pIoWatcher->events | EV_WRITE, pConn->pIoWatcher);
        // ← 不新建 pData，共用 AddIoReadEvent 创建的那一个
}
```

**结论**：一个连接经过 `AddIoReadEvent` + `AddIoWriteEvent` 之后，只有**一个** watcher 和**一个** pData，监听 `EV_READ | EV_WRITE`。

### 6.3 查看 DestroyConnect，确认 pData 在此被释放

```bash
sed -n '1596,1616p' code/Net/src/labor/Manager.cpp
```

**输出**：

```cpp
bool Manager::DestroyConnect(std::unordered_map<...>::iterator iter)
{
    ...
    DelEvent(iter->second->pIoWatcher,
             (tagManagerIoWatcherData*)iter->second->pIoWatcher->data);
    //       ↑ 这里 delete pData（见 Labor.hpp:682 的 SAFE_DELETE）
    close(iter->first);
    m_mapFdAttr.erase(iter);   // ← fd 从 map 里移除
    return(true);
}
```

### 6.4 查看 Labor.hpp 中的 DelEvent 模板

```bash
sed -n '676,685p' code/Net/include/labor/Labor.hpp
```

**输出**：

```cpp
void DelEvent(ev_timer* timer_watcher);
void DelEvent(ev_io* io_watcher);
template<typename WATCH,typename DATA>
void DelEvent(WATCH* watcher, DATA *pData)
{
    DelEvent(watcher);       // 调用 ev_io_stop
    SAFE_DELETE(pData);      // ← line 682：delete pData，此后 pData 是悬空指针
    watcher->data = nullptr;
    SAFE_DELETE(watcher);
}
```

### 6.5 查看 Labor.cpp 中的 DelEvent(ev_io*)

```bash
grep -n "DelEvent\|ev_io_stop\|ev_clear_pending" code/Net/src/labor/Labor.cpp
```

**输出**：

```
424:void Labor::DelEvent(ev_io* io_watcher)
428:    	ev_io_stop (m_loop, io_watcher);
```

此时没有 `ev_clear_pending`。

### 6.6 理解 libev 的两阶段模型

查看 libev 源码，理解 `ev_io_stop` 为何无法阻止同次 callback 的后续代码：

```bash
grep -n "ev_invoke_pending\|pending\b" code/3party/libev/ev.h | head -20
```

**输出（关键行）**：

```c
// ev.h 中 pending 相关：
#define EV_CB_INVOKE(watcher,revents) (watcher)->cb (EV_A_ (watcher), (revents))
```

```bash
grep -n "ev_invoke_pending\|p->pending\|cb_invoke" code/3party/libev/ev.c | head -30
```

**输出（关键行）**：

```c
// ev.c 中 ev_invoke_pending 的核心逻辑（简化）：
inline_speed void ev_invoke_pending (EV_P)
{
    while (pendingcnt[0])
    {
        ANPENDING *p = pendings[0] + --pendingcnt[0];
        p->w->pending = 0;            // ← 先清 pending 标记
        EV_CB_INVOKE (p->w, p->events); // ← 然后调用 callback（进入 IoCallback）
        // 此后 IoCallback 内部 ev_io_stop / ev_clear_pending
        // 已无法影响这次已经开始执行的调用
    }
}
```

**关键认知**：libev 在调用 callback **之前**已经把该 watcher 从 pending 队列取出（`p->w->pending = 0`）。callback 执行期间，再调 `ev_clear_pending` 对已执行中的这次调用无任何效果——pending 队列里已经没有它了。

---

## 七、第六步：尝试 ev_clear_pending，确认无效

### 7.1 第一次修复尝试（错误方向）

在 `Labor.cpp::DelEvent(ev_io*)` 里加 `ev_clear_pending`：

```cpp
void Labor::DelEvent(ev_io* io_watcher)
{
    if (io_watcher)
    {
        ev_clear_pending(m_loop, io_watcher);  // ← 加这行
        ev_io_stop(m_loop, io_watcher);
    }
}
```

重新编译：

```bash
cmake --build build_asan --target Center --target Hello
```

**注意**：可执行文件静态链接了 Net 代码。先验证这一点：

```bash
nm deploy/Logic/bin/Logic | grep "DelEvent"
```

**输出**：

```
00000000001a5e70 T _ZN3net5Labor8DelEventEP5ev_io
```

`T` 表示符号在本文件的 text 段——**静态链接**。仅重建 `libNet.so` 不够，必须重新链接可执行文件本身。上面的 cmake 命令已经同时重链了 Logic/Interface/Center 可执行文件。

### 7.2 验证修复是否生效

```bash
ASAN_OPTIONS=log_path=/tmp/asan_logic2 timeout 8 ./bin/Logic conf/Logic.json &
sleep 5
ls /tmp/asan_logic2.* 2>/dev/null && cat /tmp/asan_logic2.* | head -5 \
  || echo "No ASan errors"
```

**输出**：

```
/tmp/asan_logic2.92915
=================================================================
==92915==ERROR: AddressSanitizer: heap-use-after-free
READ of size 4 at 0x50200000c070 thread T0 (Logic_robot)
    #0 net::Manager::IoWrite(...)  Manager.cpp:420
```

**崩溃依然发生**。`ev_clear_pending` 无效，原因如 6.6 所述。

---

## 八、第七步：正确修复——IoCallback 加 fd 存活校验

### 8.1 修复 Manager.cpp 的 IoCallback

在 `IoRead` 调用**之前**把 `iFd` 和 `ulSeq` 保存到栈上（此时 pData 有效），调用 `IoRead` 之后通过 `m_mapFdAttr` 重新验证 fd 是否仍然存活：

```bash
# 编辑 code/Net/src/labor/Manager.cpp，修改 IoCallback 函数（第 58-77 行）
```

**修改前**：

```cpp
void Manager::IoCallback(struct ev_loop* loop, struct ev_io* watcher, int revents)
{
    if (watcher->data != nullptr)
    {
        tagManagerIoWatcherData* pData = (tagManagerIoWatcherData*)watcher->data;
        Manager* pManager = pData->pManager;
        if (revents & EV_READ)
            pManager->IoRead(pData, watcher);
        if (revents & EV_WRITE)
            pManager->IoWrite(pData, watcher);   // ← UAF
        if (revents & EV_ERROR)
            pManager->IoError(pData, watcher);
    }
}
```

**修改后**：

```cpp
void Manager::IoCallback(struct ev_loop* loop, struct ev_io* watcher, int revents)
{
    if (watcher->data != nullptr)
    {
        tagManagerIoWatcherData* pData = (tagManagerIoWatcherData*)watcher->data;
        Manager* pManager = pData->pManager;
        int iFd = pData->iFd;        // 在 IoRead 前保存，此时 pData 有效
        uint64 ulSeq = pData->ulSeq;
        if (revents & EV_READ)
        {
            pManager->IoRead(pData, watcher);
            // pData 可能已在 DestroyConnect 中被释放，不能再直接访问
            // 通过 map 重新验证 fd 是否仍然存在，且 seq 匹配（防 fd 复用）
            auto iter = pManager->m_mapFdAttr.find(iFd);
            if (iter == pManager->m_mapFdAttr.end() || iter->second->ulSeq != ulSeq)
                return;
        }
        if (revents & EV_WRITE)
            pManager->IoWrite(pData, watcher);   // 此时 pData 一定有效
        if (revents & EV_ERROR)
            pManager->IoError(pData, watcher);
    }
}
```

**`ulSeq` 的作用**：`close(fd)` 后内核可能把同一个 fd 号分配给新连接，`m_mapFdAttr.find(iFd)` 可能找到新连接的条目。通过 `ulSeq`（每次 `CreateFdAttr` 自增）区分"同一个连接"和"fd 号复用的新连接"。

### 8.2 同样修复 Worker.cpp 的 IoCallback

查看 Worker.cpp 的对应代码：

```bash
sed -n '73,92p' code/Net/src/labor/Worker.cpp
```

**输出**：

```cpp
void Worker::IoCallback(struct ev_loop* loop, struct ev_io* watcher, int revents)
{
    if (watcher->data != nullptr)
    {
        tagIoWatcherData* pData = (tagIoWatcherData*)watcher->data;
        Worker* pWorker = (Worker*)pData->pWorker;
        if (revents & EV_READ)
            pWorker->IoRead(pData, watcher);       // ← 同样的问题
        if (revents & EV_WRITE)
            pWorker->IoWrite(pData, watcher);      // ← UAF
        if (revents & EV_ERROR)
            pWorker->IoError(pData, watcher);
    }
}
```

与 Manager 完全相同的模式，Interface 的 Worker 进程崩溃就是从这里触发的。同样修复，用 `m_mapFdAttr.find(iFd)` 和 `ulSeq` 校验。

### 8.3 修改文件汇总

| 文件 | 修改内容 |
|------|----------|
| `code/Net/src/labor/Manager.cpp` | `IoCallback`：IoRead 前保存 iFd/ulSeq，IoRead 后校验 fd 存活 |
| `code/Net/src/labor/Worker.cpp` | `IoCallback`：同上（Worker 与 Manager 同构） |
| `code/Net/src/labor/Labor.cpp` | `DelEvent(ev_io*)`、`DelEvent(ev_timer*)`：各加 `ev_clear_pending`（防御层） |

---

## 九、第八步：重新编译与验证

### 9.1 重新编译

```bash
cd /home/administrator/interview-quicker/thunder
cmake --build build_asan --target Center --target Hello
```

**输出**：

```
[ 35%] Building CXX object code/Net/CMakeFiles/Center.dir/src/labor/Worker.cpp.o
[ 35%] Linking CXX executable ../../bin/Center
Deploy: deploy/Center/bin/Center
Deploy: deploy/Logic/bin/Logic + deploy/Interface/bin/Interface
[100%] Built target Hello
```

确认三个二进制都已重新链接：

```bash
ls -la deploy/{Center,Logic,Interface}/bin/*
```

**输出**：

```
-rwxr-xr-x ... Mar 22 15:28 deploy/Center/bin/Center
-rwxr-xr-x ... Mar 22 15:28 deploy/Interface/bin/Interface
-rwxr-xr-x ... Mar 22 15:28 deploy/Logic/bin/Logic
```

### 9.2 ASan 验证（单独运行 Logic）

```bash
export LD_LIBRARY_PATH="..."
cd deploy/Logic
ASAN_OPTIONS=log_path=/tmp/asan_logic_fixed timeout 8 ./bin/Logic conf/Logic.json &
LOGPID=$!
sleep 5
echo "Logic processes: $(ps aux | grep Logic_robot | grep -v grep)"
kill $LOGPID; wait $LOGPID 2>/dev/null; sleep 1
ls /tmp/asan_logic_fixed.* 2>/dev/null \
  && cat /tmp/asan_logic_fixed.* | head -5 \
  || echo "No ASan errors - fix successful!"
```

**输出**：

```
Logic processes:
  administrator  98168  Logic_robot   (Ss)   ← Manager 进程存活
  administrator  98175  Logic_robot   (S)    ← Worker 进程存活
No ASan errors - fix successful!
```

Manager 和 Worker 进程在整个 5 秒内均保持存活，ASan 无任何报告。

### 9.3 集成测试

```bash
cd deploy/tests
bash test_interfaceserver.sh
```

**输出（关键行）**：

```
=== [1/4] 启动简易注册中心 Center ===
=== [2/4] 启动 Logic 节点 ===
=== [3/4] 启动 Interface ===
=== [4/4] HTTP：GenKey → VerifyKey ===
GenKey 响应: {"code":0,"key":"..."}
VerifyKey 响应: {"code":0,"msg":"ok"}
=== 测试通过 ===
```

Interface Worker 不再崩溃，HTTP 请求正常返回。

---

## 十、根因分析总结

### 10.1 libev 事件合并与 callback 执行机制

```
阶段一：epoll_wait 返回
  fd 8 同时就绪：EV_READ | EV_WRITE（ECONNREFUSED 使读写均就绪）
  → libev 合并 revents：watcher->revents = EV_READ | EV_WRITE
  → 加入 pending 队列

阶段二：ev_invoke_pending
  p->w->pending = 0;              ← 从 pending 队列取出（此时 ev_clear_pending 无用）
  EV_CB_INVOKE(p->w, p->events);  ← 调用 IoCallback(revents = EV_READ | EV_WRITE)

  IoCallback 内部：
    ├─ IoRead(pData)
    │    └─ ECONNREFUSED → DestroyConnect → delete pData
    │
    └─ IoWrite(freed pData)  ← 同一函数调用继续执行 → heap-use-after-free
```

### 10.2 Interface 崩溃的间接路径（堆腐败滞后性）

```
Worker IoCallback(EV_READ | EV_WRITE):
  1. IoRead → DestroyConnect → free(pData)  [tagIoWatcherData, 16字节]
  2. 这 16 字节被内存分配器复用
     → 分配给 log4cplus::InternalLoggingEvent 的某个字段
  3. IoWrite(freed pData) → 把 HTTP 报文原始字节写入该内存
     → 覆盖 InternalLoggingEvent 的 vtable 和 loggerName 指针
     → 此刻堆腐败已发生，但进程还在继续运行
  4. 下一次 HttpCodec::Decode 调用 LOG4_TRACE：
     → 访问已损坏的 InternalLoggingEvent
     → loggerName 内部指针 = 0x65746e492f205453（"ST /Inte"）
     → memcpy 写入该非法地址 → SIGSEGV
```

---

## 十一、经验总结

### 11.1 调试工具链对照

| 阶段 | 命令 | 发现了什么 | 局限 |
|------|------|-----------|------|
| 初步 | `tail -60 Interface_robot_W0.log` | PID 变化，日志中断 | 只知道进程崩溃，不知原因 |
| 确认类型 | `dmesg \| tail -20` | SIGSEGV in libc，堆腐败特征 | 无调用栈 |
| 获取 core | `sysctl kernel.core_pattern=core.%p` | 绕过 WSL pipe，落盘 | 需要重新触发崩溃 |
| 崩溃栈 | `gdb -batch -ex 'bt full' Interface core.N` | 完整调用栈，损坏的地址值 | 只看到"踩雷"点 |
| 地址解析 | 手动 ASCII 转换 `0x65746e492f205453` | HTTP 数据出现在指针字段，确认堆腐败 | 仍无越界写位置 |
| 根因 | `ASAN_OPTIONS=log_path=... ./Logic` | **精确的越界写/读位置**，三条完整调用链 | 需重新编译，性能约降 2~5 倍 |
| 规避陷阱 | `ldd deploy/lib/libUtil.so \| grep asan` | ASan .so 污染 deploy/lib，导致 Center 无法启动 | — |
| 静态链接验证 | `nm deploy/Logic/bin/Logic \| grep DelEvent` | Net 代码静态链接进了可执行文件 | 仅改 .so 不够，需重链二进制 |

### 11.2 可复用的调试要点

1. **堆腐败必用 ASan**：崩溃点不是根因，`gdb + core` 看不到第一次越界写。堆腐败类 bug 若不用 ASan，纯靠代码 review 排查效率极低。

2. **ASan 构建要隔离**：用独立目录（`build_asan/`），不要让 ASan 版的 `.so` 污染 `deploy/lib/`，否则未用 ASan 编译的其他进程会因为加载顺序问题无法启动。

3. **服务进程用 `ASAN_OPTIONS=log_path`**：fork 型服务进程会产生多个进程，直接写 stderr 会混乱。`log_path=/tmp/asan_x` 让每个进程写独立文件。

4. **静态链接 vs 动态链接**：修改 `.cpp` 后，用 `nm binary | grep Symbol` 确认符号是 `T`（本文件 text 段，即静态链接）还是 `U`（未定义，即动态链接）。静态链接时必须重新链接可执行文件，更新 `.so` 不起作用。

5. **libev callback 内的资源生命周期**：在 libev 的 `ev_io` callback 内销毁 watcher（`ev_io_stop` + `delete pData`）之后，不能再访问 `pData`。`ev_io_stop` 阻止未来事件，`ev_clear_pending` 清除 pending 队列中尚未执行的条目，**两者均无法阻止已在执行中的同一次 callback 继续访问已释放的内存**。只能在应用层提前保存标识符，调用后重新验证有效性。

---

## 十二、第二个 Bug：C++20 协程自毁 UAF（StepCo20）

### 12.1 现象

IoCallback UAF 修复并重新编译后，`GenKey → VerifyKey` 测试仍然触发崩溃。表现与之前相同：`curl: (52) Empty reply from server`，Worker 进程在约 1 秒内重启。

日志中出现两类特征：

1. **log4cplus 路径乱码**：部分日志行的 `__FILE__` 路径变为带空格前缀（` home/...`）或被截断（`er.cpp:478`），表明进程堆内存已被破坏。

2. **崩溃点固定在 `HttpCodec::ToString()`**：不论 GenKey 还是 VerifyKey，崩溃必然发生在 `HttpCodec.cpp:710`（`ToString()` 函数入口 log 之后）。GenKey 请求可以成功响应（在协程完成路径上），但 VerifyKey 请求因为堆腐败积累到此处时触发 SIGSEGV。

### 12.2 排查过程

**关键日志序列**（节选自 `Interface_robot_W0.log`）：

```
# GenKey 步骤完成后：
[Worker.cpp:1395] DeleteCallback(Step* 0x769e1961e6e0)   ← 步骤对象被删除
[Worker.cpp:444]  IoRead()
[Worker.cpp:478]  recv from fd 7, data len 0             ← GenKey 连接关闭（EOF）
[Worker.cpp:4573] DestroyConnect() timer ev_timer_stop

# VerifyKey 到达：
[Worker.cpp:478]  recv from fd 7, data len 234           ← 正常收到数据
[ HttpCodec.cpp:58]  Decode() (路径已乱码)
[ HttpCodec.cpp:77]  Decode() readable=234  (乱码)
[virtual...HttpCodec::Decode p:645] Decode()  (严重乱码)
[HttpCodec.cpp:710]  ToString()              ← 进入函数，log 正常
                                             ← CRASH（下一条日志是"program begin"）
```

乱码从 `DestroyConnect()` 之后就开始出现（行 276），说明堆腐败发生在 GenKey 连接关闭阶段，而不是 VerifyKey 到达时。

**定位堆腐败源头**：结合 `StepCo20::Emit` 的协程启动代码，发现外层 lambda 在 `CoroutineMain()` 完成后调用了 `GetLabor()->ExecStep(this, 0.0)` 来触发清理，该调用会重入 `Emit`，进而执行 `m_oAsyncBootstrap.reset()`，而此时外层 lambda 协程帧**仍在调用栈上运行**。

### 12.3 根因：对运行中协程调用 `destroy()`

**调用链（有问题的版本）**：

```
StepCo20::Emit() 首次调用
  → m_oAsyncBootstrap.emplace(coroTask())
  → 启动外层 lambda (AsyncTask 协程)
  → co_await CoroutineMain()           ← 挂起，等待 Logic 响应

Worker::Dispose → pStep->Callback()
  → m_coroHandle.resume()             ← 恢复 CoroutineMain
  → CoroutineMain 运行完毕，响应已发送
  → 外层 lambda 继续执行：
      m_bCoroutineCompleted = true
      m_bCoroutineRunning   = false
      GetLabor()->ExecStep(this, 0.0) ← [!] 外层 lambda 还在调用栈上！

ExecStep(this, 0.0)
  → RegisterCallback(this, 0.0)
  → Emit(0)                           ← 重入，m_bCoroutineCompleted == true
      → m_oAsyncBootstrap.reset()     ← AsyncTask::~AsyncTask() → coro_.destroy()
                                      ← [!] 对"运行中"的协程调用 destroy() = UB
      → DeleteCallback(this)          ← delete this（step 对象被销毁）

ExecStep 返回给外层 lambda
  → 外层 lambda 尝试执行 final_suspend  ← 协程帧已被 free（UAF）
  → 堆内存被破坏
```

**两个叠加问题**：

1. **对运行中的协程帧调 `destroy()`**：C++20 标准明确规定 `coroutine_handle::destroy()` 只能在协程**已挂起**时调用，对运行中的协程调用是未定义行为。在 GCC/Clang 实现中，这会立即 free 堆上的协程帧，但调用栈仍然存在，当前函数返回时会访问已释放的帧内存。

2. **`AsyncTask::final_suspend()` 返回 `suspend_never`（原始设计）**：协程自然结束时，`suspend_never` 使运行时自动销毁协程帧，与 `m_oAsyncBootstrap` 析构时再次调用 `coro_.destroy()` 形成**双重 free**。

```
AsyncTask::promise_type::final_suspend() → suspend_never
  → 协程结束时运行时自动 free 帧
AsyncTask::~AsyncTask()
  → if (coro_) coro_.destroy()  ← 对已销毁的帧再次 free → double-free
```

### 12.4 修复方案

**修改一：`Coroutine20.hpp` — `final_suspend` 改为 `suspend_always`**

```cpp
// 修复前
std::suspend_never final_suspend() noexcept { return {}; }

// 修复后
std::suspend_always final_suspend() noexcept { return {}; }
```

使外层 lambda 完成时挂起而不是自毁，协程帧保持存活，由 `~StepCo20()` 经过 `m_oAsyncBootstrap` 析构统一销毁——此时协程已处于挂起状态，`destroy()` 是合法操作。

**修改二：`StepCo20.cpp` — 外层 lambda 移除重入调用**

```cpp
// 修复前
co_await CoroutineMain();
m_bCoroutineCompleted = true;
m_bCoroutineRunning   = false;
GetLabor()->ExecStep(this, 0.0);  // ← 触发 UB 的根源

// 修复后
co_await CoroutineMain();
m_bCoroutineCompleted = true;
m_bCoroutineRunning   = false;
OnCoroutineComplete(true);        // 直接调用，不重入 Emit
// 外层 lambda 自然结束 → final_suspend(suspend_always) → 帧挂起
```

**修改三：`StepCo20.cpp` — `Callback` 检测协程完成并返回 `STATUS_CMD_COMPLETED`**

```cpp
// 修复前：Callback 始终返回 STATUS_CMD_RUNNING，由 ExecStep(this,0) 触发清理
if (m_coroHandle && !m_coroHandle.done())
    m_coroHandle.resume();
return STATUS_CMD_RUNNING;

// 修复后：resume() 驱动完整链路（对称转移），返回后检查完成标志
if (m_coroHandle && !m_coroHandle.done())
    m_coroHandle.resume();
if (m_bCoroutineCompleted)
    return STATUS_CMD_COMPLETED;  // Worker::Dispose 调用 DeleteCallback，安全销毁 step
return STATUS_CMD_RUNNING;
```

### 12.5 修复后的协程生命周期

```
StepCo20::Emit() 首次调用
  → m_oAsyncBootstrap.emplace(coroTask())
  → 外层 lambda 启动，co_await CoroutineMain() 挂起

Worker::Dispose → pStep->Callback()
  → m_coroHandle.resume()
  → [对称转移链] CoroutineMain → 外层 lambda
  → 外层 lambda: OnCoroutineComplete(true)（发送 HTTP 响应）
  → 外层 lambda 结束 → final_suspend(suspend_always) → 帧挂起 ✓
  → resume() 返回
  → m_bCoroutineCompleted == true → return STATUS_CMD_COMPLETED

Worker::Dispose 调用 DeleteCallback(step)
  → 从 m_mapCallbackStep 中移除
  → unique_ptr 析构 → delete step
  → ~StepCo20() → m_oAsyncBootstrap 析构 → ~AsyncTask()
  → coro_.destroy()  ← 对【已挂起】协程调用，合法 ✓
```

### 12.6 对应的 C++20 规范要点

| 操作 | 前提条件 | 违规后果 |
|------|---------|---------|
| `coroutine_handle::destroy()` | 协程必须处于**挂起**状态（suspended 或 final-suspended） | 对运行中协程调用 = UB，实际表现为 free 正在使用的栈帧 |
| `suspend_never` 的 `final_suspend` | 协程自然完成后运行时自动 free 帧 | 若持有者再次析构调用 `destroy()`，发生 double-free |
| `coroutine_handle::resume()` | 协程必须处于**挂起**且**未完成**状态 | 对完成的协程调用 resume = UB |

### 12.7 验证

```bash
cmake --build build --target Hello -j$(nproc)
cmake --install build
bash deploy/tests/test_interfaceserver.sh
```

```
=== GenKey 响应 ===
{"code":0,"token":"7441429424580395009","key":"7441429424580395010"}
=== VerifyKey 响应 ===
{"code":0}
=== VerifyKey 完成 ===
```

Worker 进程不再崩溃，两次请求均正常完成。

### 12.8 可复用经验

1. **C++20 协程不能自毁**：协程在自身的执行路径上不能通过任何间接方式触发 `coroutine_handle::destroy()`（包括通过持有者对象的析构）。合法的销毁时机只有两个：`final_suspend` 返回 `suspend_never`（运行时自动销毁）或协程挂起后由外部显式 `destroy()`，二者选一，不可叠加。

2. **`suspend_never` + 外部持有 = 设计陷阱**：若用 `suspend_never` 作为 `final_suspend`，意图让运行时管理帧生命周期，就不能同时用 RAII 对象（`optional<AsyncTask>`、`unique_ptr` 等）持有协程句柄——析构时的二次 `destroy()` 必然 double-free。两种模式只能选其一：要么 `suspend_never`（无需外部 RAII），要么 `suspend_always`（由 RAII 统一析构）。

3. **重入路径要警惕**：从协程体内部调用可能重入 `Emit` 的框架函数（`ExecStep`、`DeleteCallback` 等）时，要仔细检查重入路径是否会触发对当前协程帧的销毁。最稳妥的做法是：协程体内只做业务逻辑，生命周期管理（注册/注销/删除）交由框架的 callback 返回值机制来驱动。
