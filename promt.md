# 「文武分治」策略：AI 协作架构设计实践

为了实现最高性价比和最强架构设计能力的平衡，建议采用「文武分治」策略：

- **总架构师**：Claude 4.6 Opus（出图纸）
- **高级开发工程师**：Cursor Composer + Claude 4.6 Sonnet（写代码）

以下是 2026 年最推荐的高效操作流程。

---

## 第一阶段：架构设计（由 Opus 负责）

| 项目 | 说明 |
|------|------|
| **工具** | Cursor Chat (Cmd+L) |
| **模型** | 切换到 Claude 4.6 Opus |
| **全局索引** | 在 Chat 窗口输入 `@Codebase`，让 Opus 扫描整个项目结构 |

### 下达指令

> 请分析当前 [XX 模块] 的架构。我计划将其重构为 [例如：领域驱动设计 DDD / 插件化架构]。请为我生成一份详细的《重构执行方案.md》，要求包含：
>
> 1. 核心逻辑的拆解步骤
> 2. 文件依赖关系的变更列表
> 3. 具体的接口定义（Interface/Types）
> 4. 分步实施计划（第一步做什么，第二步做什么）
>
> **注意**：先不要写完整的代码，只出架构蓝图。

### 保存蓝图

将 Opus 生成的内容保存到项目根目录下的 `REFACTOR_PLAN.md`。

---

## 第二阶段：代码实现（由 Sonnet 负责）

| 项目 | 说明 |
|------|------|
| **工具** | Cursor Composer (Cmd+I) |
| **模型** | 切换到 Claude 4.6 Sonnet |
| **引用蓝图** | 在 Composer 窗口中，先通过 `#` 引用刚才的文件 |

### 分步执行

> 根据 @重构执行方案.md 中的**第一步**，请帮我重写 [A 文件] 和 [B 文件]。确保符合方案中定义的接口规范和设计模式。

### 一键应用

Sonnet 会极快地生成多文件修改方案，你只需点击「Accept all」。

---

## 为什么这样处理效率最高且最省钱？

### 1. 节省「脑力」额度

- Opus 的额度昂贵且响应慢，只让它处理「需要深度思考」的架构设计，几轮对话就能定稿
- Sonnet 响应极快，且 4.6 版本的代码实现能力与 Opus 极度接近，用它来执行具体的增删改查，体验最流畅

### 2. 逻辑一致性

- 通过 `REFACTOR_PLAN.md` 这个「中间文件」，你为 AI 建立了一个事实来源（Source of Truth）
- 如果 Sonnet 在编写过程中跑偏了，只需提醒它：「看方案第三章，你这里的依赖关系搞错了」，它能立刻修正

### 3. 上下文管理

- 架构设计需要 `@Codebase` 这种大上下文
- 代码编写只需要针对具体文件的局部上下文
- 分两步走，能有效避免 AI 因为一次性处理太多文件而产生的「幻觉」

---

## 专业提示 (Pro Tip)

如果你的重构任务非常庞大（超过 50 个文件），建议在《重构执行方案.md》中要求 Opus 给每一段逻辑写一个「重构检查清单（Checklist）」。这样在用 Sonnet 编写时，你可以每写完一个模块就让它对照清单自查。

---

您现在的重构任务主要涉及哪些技术栈？（例如：Java 后端、React 前端或是 Python 数据处理）我可以帮您优化一下针对该技术栈的架构设计提示词 (Prompt)。
