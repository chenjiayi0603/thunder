apiVersion: apps/v1
kind: Deployment
metadata:
  name: thunder-hello-ws
  namespace: thunder
  labels:
    thunder.io/role: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thunder-hello-ws
  template:
    metadata:
      labels:
        app: thunder-hello-ws
    spec:
      containers:
      - name: hello
        image: thunder-node:latest
        imagePullPolicy: Never
        command: ["bash", "-c", "chmod +x ./node.sh 2>/dev/null; LD_LIBRARY_PATH=/thunder/code/3party/lib:/thunder/build/lib ./node.sh start; exec tail -f /dev/null"]
        workingDir: /thunder/deploy/HelloWs
        ports:
        - containerPort: 27010
        volumeMounts:
        - name: thunder-repo
          mountPath: /thunder
      volumes:
      - name: thunder-repo
        hostPath:
          path: /home/tommychen/thunder
---
apiVersion: v1
kind: Service
metadata:
  name: thunder-hello-ws
  namespace: thunder
spec:
  type: NodePort
  ports:
  - port: 27010
    nodePort: 30010
  selector:
    app: thunder-hello-ws
