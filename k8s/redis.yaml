apiVersion: apps/v1
kind: Deployment
metadata:
  name: thunder-redis
  namespace: thunder
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thunder-redis
  template:
    metadata:
      labels:
        app: thunder-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: thunder-redis
  namespace: thunder
spec:
  ports:
  - port: 6379
  selector:
    app: thunder-redis
