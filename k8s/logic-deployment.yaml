apiVersion: apps/v1
kind: Deployment
metadata:
  name: thunder-logic
  namespace: thunder
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thunder-logic
      version: v1
  template:
    metadata:
      labels:
        app: thunder-logic
        version: v1
    spec:
      containers:
      - name: app
        image: thunder-logic:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: NODE_VERSION
          value: "v1"
        - name: INNER_PORT
          value: "16068"
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        ports:
        - containerPort: 16068
