apiVersion: apps/v1
kind: Deployment
metadata:
  name: thunder-hello
  namespace: thunder
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thunder-hello
  template:
    metadata:
      labels:
        app: thunder-hello
    spec:
      containers:
      - name: app
        image: ubuntu:26.04
        imagePullPolicy: IfNotPresent
        workingDir: /thunder/deploy/HelloHttp
        env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: LD_LIBRARY_PATH
          value: "/thunder/build/lib:/thunder/deploy/3lib:/thunder/code/3party/lib"
        - name: DEBIAN_FRONTEND
          value: noninteractive
        command:
        - bash
        - -c
        - |
          apt-get update -qq && apt-get install -y -qq --no-install-recommends ca-certificates net-tools procps iproute2 libjemalloc2 libbrotli1 libsnappy1v5 liburing2 2>/dev/null
          chmod +x ./node.sh 2>/dev/null || true
          mkdir -p /tmp/conf && cp /thunder/k8s/conf/Hello.json /tmp/conf/Hello.json && sed -i "s|0.0.0.0|$POD_IP|g" /tmp/conf/Hello.json
          LD_LIBRARY_PATH=$LD_LIBRARY_PATH bin/HelloHttp /tmp/conf/Hello.json
          exec tail -f /dev/null
        ports:
        - containerPort: 27006
        volumeMounts:
        - name: thunder-repo
          mountPath: /thunder
        - name: thunder-plugins
          mountPath: /data/thunder/plugins
          readOnly: true
      volumes:
      - name: thunder-repo
        hostPath:
          path: /home/tommychen/thunder
      - name: thunder-plugins
        persistentVolumeClaim:
          claimName: thunder-plugins
---
apiVersion: v1
kind: Service
metadata:
  name: thunder-hello
  namespace: thunder
spec:
  type: NodePort
  ports:
  - port: 27006
    nodePort: 30006
  selector:
    app: thunder-hello
