apiVersion: apps/v1
kind: Deployment
metadata:
  name: thunder-hello
  namespace: thunder
  labels:
    thunder.io/role: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thunder-hello
  template:
    metadata:
      labels:
        app: thunder-hello
    spec:
      containers:
      - name: hello
        image: thunder-node:latest
        imagePullPolicy: Never
        env:
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        command: ["bash", "-c", "chmod +x ./node.sh 2>/dev/null; LD_LIBRARY_PATH=/thunder/code/3party/lib:/thunder/build/lib ./node.sh start; exec tail -f /dev/null"]
        workingDir: /thunder/deploy/HelloHttp
        ports:
        - containerPort: 27006
        volumeMounts:
        - name: thunder-repo
          mountPath: /thunder
      volumes:
      - name: thunder-repo
        hostPath:
          path: /home/tommychen/thunder
---
apiVersion: v1
kind: Service
metadata:
  name: thunder-hello
  namespace: thunder
spec:
  type: NodePort
  ports:
  - port: 27006
    nodePort: 30006
  selector:
    app: thunder-hello
