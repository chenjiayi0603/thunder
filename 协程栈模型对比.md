# 协程栈模型对比

本文档说明两类**栈式协程**（A：共享+拷贝，B：独立栈）的机制差异；**§3** 给出本仓库实际并存的两套方案——**栈式（util）与 C++20 无栈**——的**特性对照（优缺点）**，**§3.4** 回答「是否 C++20 一定更好」的结论性表述。

- **栈式 A**：共享执行栈 + 挂起时拷贝 —— 见 `code/Util/src/coroutine/coroutine.cpp`。
- **栈式 B**：每协程独立栈（独占栈）。
- **C++20**：无栈协程 —— 项目内参考 `code/Net/include/step/Coroutine20.hpp` 中的 `net::Task`、`BaseTask` 等。

---

## 1. 共享执行栈 + 拷贝保存（栈式 A，本仓库 util 实现）

**代表**：云风 `coroutine`、`coroutine.cpp`。

| 维度 | 说明 |
|------|------|
| **执行时** | 同一 `schedule` 内**只有一块**固定大小的执行栈（如 `S->stack`，默认 1MB），**任意时刻仅当前运行的协程**占用。 |
| **挂起时** | `yield` 将**当前已使用的栈区间** `memcpy` 到该协程私有的堆缓冲区 `C->stack`（按需扩容）。 |
| **恢复时** | 把 `C->stack` 拷回共享执行栈的对应位置，再 `swapcontext` 继续执行。 |
| **内存特点** | 峰值上通常**省**：不必为每个协程长期预留一整块大栈；挂起协程只保留**实际栈用量**的副本。 |
| **切换成本** | **高**：每次 `yield`/`resume`（挂起路径）往往伴随**与栈用量成正比**的内存拷贝。 |
| **风险/约束** | 单协程运行时栈深度不能超过共享执行栈总大小；需保证指针等不指向已拷走区域内的悬空地址；同一线程驱动同一 `schedule`（见 `code/Util/src/coroutine/README.md`）。 |

**一句话**：**跑的时候共用一块栈；睡的时候各自一份栈快照在堆上。**

---

## 2. 每协程独立栈（栈式 B，独占栈）

**代表**：许多 fiber / 用户态线程库：为每个协程 `mmap`/`malloc` 一块**固定大小**的栈（如各 1MB～2MB），`ucontext` 或汇编切换时**始终**把 CPU 栈指针切到该块内存。

| 维度 | 说明 |
|------|------|
| **执行时** | 每个协程在**自己的栈区间**上运行，与是否挂起无关。 |
| **挂起时** | 一般**不拷贝整段栈**，只做上下文（寄存器、栈指针等）切换。 |
| **恢复时** | 切回该协程的栈指针与上下文即可。 |
| **内存特点** | **N 个协程 ≈ N 份常驻栈空间**（即使栈大部分未使用），总量容易偏大。 |
| **切换成本** | **相对低**：通常无大块 `memcpy`，主要是上下文切换开销。 |
| **风险/约束** | 需为栈溢出做 guard page 或探测；协程数量很大时内存压力更明显。 |

**一句话**：**每人一块固定栈，切换多半只换上下文、不整段拷栈。**

---

## 3. 两者特性对比：栈式（util）与 C++20 无栈（优缺点）

**对比对象**：

- **栈式**：本仓库 `util/coroutine`（**栈式 A**：共享执行栈 + `yield` 时拷栈保存），经 `net::Coroutine` 等封装使用。
- **C++20 无栈**：`net::Task` / `AsyncTask`、`BaseTask` 等（协程帧在堆上，挂起点在 `co_await`）。

### 3.1 栈式协程（util）

| 优点 | 缺点 |
|------|------|
| **不依赖 C++20**，老工具链可用 | **`yield`/`resume` 路径常伴随大块 `memcpy`**，栈深或切换频时 CPU、带宽开销明显 |
| **真栈式**：深层普通调用里也可 `yield`，迁旧代码时往往**只插切换点** | **共享执行栈有固定上限**（如 1MB），单协程栈过深会溢出 |
| **API 简单**（函数指针 + `void*`），易理解 | **`void*` 与手工生命周期**，类型安全与异常传播弱于语言级协程 |
| **同调度器内多协程**主要靠「挂起拷走栈」，**总内存常优于「每协程一大块独占栈」（栈式 B）** | **`ucontext` 可移植性/维护性**因平台而异；须遵守「同线程 `open`/`resume`」等约束 |
| | **与事件驱动结合**要自己接好「谁在何时 `resume`」，无标准 `awaiter` 形态 |

### 3.2 C++20 无栈协程

| 优点 | 缺点 |
|------|------|
| **挂起/恢复通常无整段栈拷贝**，高频 `co_await` 往往比栈式 A **更省切换成本** | **必须 C++20** 及编译器支持 |
| **`Task<T>`、`co_await` 可组合**，返回类型与异常路径相对清晰 | **无栈**：不能在任意非协程函数里直接挂起，常需**整条异步链**改为可等待类型 |
| **`await_suspend` + `handle.resume()`** 与 **I/O 回调**对齐自然（如 `BaseTask`） | **promise、帧生命周期、悬空引用**等需规范使用；调试栈不如普通函数直观 |
| **标准 `<coroutine>`**，长期不绑 `ucontext` | **协程帧多在堆上分配**（视编译器/优化），热点路径需关注分配次数 |
| | **与 util 栈式协程混用**时若边界不清，易出现调度与线程安全混乱 |

### 3.3 与「栈式 B：每协程独立栈」的相对位置（补充）

- **栈式 B** 未在本仓库采用；其 **切换** 往往优于 A（无拷栈），**常驻内存** 往往在协程很多时劣于 A 与 C++20 轻量帧。
- C++20 相对 B 的常见优势仍是：**不必每人预留一整块大栈**、**标准语义**；相对 A 的优势侧重：**无拷栈**与 **类型化异步组合**。

### 3.4 是否「现代 C++20 协程更好」？

**结论：没有统一的「更好」，只有是否更合适当前约束与场景。**

| 更倾向用 **C++20 无栈** 时 | 更倾向保留 **栈式（util）** 或其它栈式方案时 |
|---------------------------|---------------------------------------------|
| 已上 C++20，要写**新的**异步/IO 链路与 `co_await` 组合 | **不能**要求 C++20，或工具链/第三方限制在旧标准 |
| **大量**协程、**高频**挂起恢复，希望避免栈式 A 的 **拷栈** | 遗留代码**调用栈很深**，只想在少数点 **`yield`**，不愿整条链改成 `Task` |
| 希望与 **回调/`resume` 事件驱动** 对齐（如 `BaseTask` 模式） | 已有 **`net::Coroutine` + Labor** 驱动路径，改写成本高且收益不明确 |
| 重视 **类型与标准语义**（`Task<T>`、`<coroutine>`） | 依赖 **`ucontext` 已跑通**且平台固定，短期以稳定为先 |

**本仓库**：同时保留 **util 栈式** 与 **C++20 `Task`/任务体系** 是常见做法——**新异步逻辑**可逐步走 C++20；**旧路径或未重构模块**不必强行一刀切。是否「更好」以**团队标准、迁移成本、性能剖面**为准，不宜口号化。


在大量异步、频繁挂起、要类型组合和跟事件回调对接时，C++20 无栈协程通常更合适；在特定约束下，栈式（你们 util 那套）仍有优势。

C++20 更占优的典型情况

新写的异步链路、co_await 能从头设计。
挂起/恢复很频繁，不想承受 按栈深 memcpy。
想要 Task<T>、异常、awaiter 这类语言级抽象。
长期维护、跟标准与工具链对齐。
栈式（util）仍可能更合理的情况

不能上 C++20 或要兼容很老的编译环境。
遗留代码里调用栈很深，只想 局部插 yield，不愿整条链改成可等待函数。
挂起不频繁、栈不深，拷栈成本可接受，而 改造成 C++20 成本很高。
所以：不是「现代 C++ 绝对更好」，而是「问题域和迁移成本决定」——新项目、以异步 I/O 为主、团队能统一 C++20，倾向 C++20；老代码、深栈同步逻辑、强兼容需求，栈式仍是务实选项。你们文档里 §3 的优缺点表就是在表达这个取舍，而不是判胜负。

---

## 4. 三者选型简表

| 对比项 | 栈式 A：共享+拷贝 | 栈式 B：独立栈 | C++20 无栈 |
|--------|-------------------|----------------|------------|
| 极大量轻量任务、高频切换 | 拷贝开销易成瓶颈 | 内存总和易大 | **往往更均衡** |
| 老代码里深栈、插一点就让出 | **改动小** | 可行 | 需改调用链为可等待 |
| 内存峰值（多协程） | 常优于 B | 易偏高 | 通常优于 B，视帧大小 |
| 切换路径大内存操作 | 有（拷栈） | 一般无 | **无整栈拷贝** |
| 编译器 / 标准 | C++11 亦可 | 同左 | **需 C++20** |
| 本仓库 | `util/coroutine` | 未采用 | `Coroutine20.hpp` / `BaseTask` |

---

## 5. 相关源码位置

- 栈式 A：`code/Util/src/coroutine/coroutine.cpp`、`coroutine.h`；约束见同目录 `README.md`。
- C++20：`code/Net/include/step/Coroutine20.hpp`（`Task` / `AsyncTask`），`code/Net/include/task/BaseTask.hpp` 及 `BaseTask.cpp`。
